package Legacy.Manager;

import ClientSide.ClientApp;
import Field.Point2D;
import java.util.Random;
import java.util.concurrent.ConcurrentHashMap;

public class ClientManager {
    //クライアント管理
    //public static final ConcurrentHashMap<Integer, ClientApp> clientMap = new ConcurrentHashMap<>();



    /*
    public static void printAllClients(){
        for (int clientId : ClientManager.clientMap.keySet()) {
            System.out.println(clientMap.get(clientId));
        }
    }
    */


}
